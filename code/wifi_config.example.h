#pragma once

// Copy this file to wifi_config.h and replace the placeholders locally.
// wifi_config.h is ignored by Git so real credentials are never committed.
#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASS "your-wifi-password"
